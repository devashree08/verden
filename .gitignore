Pagination: set `limit` (default 1000, max 15000) and `offset` (start at 0). 
To get more rows, call again with `offset = previous_offset + limit`. 
See `fields://get_all_apps_value_by_user` for column definitions.
