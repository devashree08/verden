log.info(
        "OIDC config resolved",
        extra={"extra": {
            "issuer_set": bool(OIDC_ISSUER_URI),
            "jwks_set": bool(OIDC_JWKS_URI),
            "aud_set": bool(OIDC_ALLOWED_AUDIENCE),
        }},
    )
