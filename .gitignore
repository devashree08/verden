// inside component
const handleExport = React.useCallback(() => {
  return exportAttributeMappingExcel({
    filters,
    selectedSubcatID,             // works from both views; pass it even if null
  });
}, [filters, selectedSubcatID]);
