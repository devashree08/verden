# Runtime Config (Server Layer)
- Non-secret env knobs (e.g., DB_BACKEND, OIDC_ISSUER_URI, OIDC_ALLOWED_AUDIENCE).
- Secrets are provided at deploy time via Secret Manager -> env (Cloud Run).
- Local `.env` is for placeholders only. Never commit real secrets.
