     {header?.note ? (
+          <Button
+            size={size}
+            variant="text"
+            onClick={() => setViewNoteOpen(true)}
+            disabled={saving}
+          >
+            View note
+          </Button>
+        ) : null}




+      {/* Read-only note viewer */}
+      <Dialog open={viewNoteOpen} onClose={() => setViewNoteOpen(false)} fullWidth maxWidth="sm">
+        <DialogTitle>Sign-off note</DialogTitle>
+        <DialogContent>
+          <Box component="pre" sx={{ whiteSpace: 'pre-wrap', fontFamily: 'inherit', m: 0, mt: 1 }}>
+            {header?.note || '—'}
+          </Box>
+        </DialogContent>
+        <DialogActions>
+          <Button onClick={() => setViewNoteOpen(false)}>Close</Button>
+        </DialogActions>
+      </Dialog>

