# Deploy Notes (to use once security approves)
- Build with Cloud Build -> Artifact Registry (CMEK)
- Deploy to Cloud Run (CMEK) with Direct VPC egress or Serverless VPC connector
- Map secrets to env, set non-secret env knobs
